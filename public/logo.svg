<svg width="200" height="60" viewBox="0 0 200 60" xmlns="http://www.w3.org/2000/svg">
  <!-- Circle background -->
  <circle cx="30" cy="30" r="25" fill="#000000" />

  <!-- Stylized 'E' inside the circle -->
  <g fill="#FFFFFF">
    <!-- Top bar with curve -->
    <path d="M15 18 Q25 12 35 18 H15 Z" />
    <!-- Middle bar -->
    <rect x="15" y="27" width="20" height="4" />
    <!-- Bottom bar with curve -->
    <path d="M15 36 Q25 42 35 36 H15 Z" />
  </g>

  <!-- EDMICH text -->
  <text x="70" y="38" font-family="system-ui, -apple-system, sans-serif" font-size="28" font-weight="900" fill="#1E40AF">
    EDMICH
  </text>
</svg>
